<div class="row">
    <main class="col-md-12">
        <br>
        <h2>{{viewData.student.firstName}} {{viewData.student.lastName}} - Student: {{viewData.student.studentNum}}</h2>
        <hr />
        <form>
            <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Name</label>
                            <input class="form-control" name="Name" type="text" value="{{viewData.student.firstName}}" />
                        </div>
                    </div>
            <div class="row">
                <div class="col-md-3">
                    <div class="form-group">
                        <label for="studentNum">Student Number</label>
                        <input class="form-control" id="studentNum" name="studentNum" type="text" readonly value="{{student.studentNum}}" />
                    </div>
                </div>
                <div class="col-md-9">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input class="form-control" id="email" name="email" type="text" readonly value="{{student.email}}" />
                    </div>
                </div>
                <div class="col-md-12">
                    <div class="form-group">
                        <label for="address">Address</label>
                        <input class="form-control" id="address" name="address" type="text" readonly value="{{student.addressStreet}}, {{student.addressCity}}, {{student.addressProvince}}" />
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label for="status">Status</label>
                        <input class="form-control" id="status" name="status" type="text" readonly value="{{student.status}}" />
                    </div>
                </div>
                <div class="col-md-4">
                        <div class="form-group">
                            <label for="course">Course:</label>
                            <select class="form-control" name="courseId" id="course">
                                <option value="">Select a course</option>
                                {{#each viewData.courses}}
                                    <option value="{{courseId}}" {{#equal ../viewData.student.course courseId}}selected{{/equal}}>{{courseCode}}</option>
                                {{/each}}
                            </select>
                        </div>
                    </div>
                </div>

            </div>
        </form>
    </div>
</div>
